# FileBot Organizer

Home Assistant add-on that watches a folder for new media files and organizes them with FileBot.

The add-on supports:

- Instant file watching with inotify
- Polling fallback mode
- Optional direct mounting of local disks such as `/dev/sda1`
- Configurable FileBot database, action, conflict mode, and naming format
